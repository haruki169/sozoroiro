# そぞろいろ 実装タスクリスト

作成日: 2026-08-16
更新日: 2026-08-17（design.md / tech-stack.md の確定事項反映に伴うタスク更新）
前提ドキュメント: `そぞろいろ_要件定義書.md`、`tech-stack.md`、`docs/design.md`（いずれもリポジトリルート/docs配下）

タスク粒度は概ね半日〜1日程度を目安とする。要件定義書6章のMust機能から着手し、Should・Coulds は後半フェーズに配置している。

**2026-08-17の更新について**: `tech-stack.md`・`docs/design.md`の未決事項がユーザー決定によりすべて確定したことに伴い、T-004（DBスキーマ）・T-005（お題カタログ件数）・T-006（お題選定ロジック）・T-013（過去お題再訪）・T-017（通知スケジューリング）・T-018（色の地図）の完了条件を更新し、新規タスク T-023（MapTiler APIキー取得・設定）・T-024（iOS配布準備）を追加した。なお、実装はまだ着手されていないため、`deletedAt`カラム追加等のデータモデル変更は初期スキーマ（schemaVersion=1）にそのまま反映すればよく、別途DBマイグレーションタスクは発生しない。

各タスクはGitHub Issuesでも管理する（本リポジトリのIssues参照）。本ファイルはIssue化の元となるソース・オブ・トゥルースとして更新し続ける。

---

## フェーズ0: プロジェクト基盤構築

### T-001: Flutterプロジェクト初期セットアップ
- **関連要件**: tech-stack.md 全体、要件定義書1章（プラットフォーム: スマートフォンアプリ）
- **完了条件**:
  - `flutter create` によりiOS/Android両対応プロジェクトが作成されている
  - Riverpod、go_router、Drift（drift, drift_dev, sqlite3_flutter_libs）、path_provider、image_picker、palette_generator、geolocator、flutter_map、flutter_local_notifications、share_plus 等、design.md/tech-stack.mdで挙げた主要パッケージが `pubspec.yaml` に導入され、`flutter pub get` が通る
  - design.md 5.1のディレクトリ構成（`lib/core`, `lib/domain`, `lib/data`, `lib/presentation`）が作成されている
  - `analysis_options.yaml` に lint 設定がされている
  - 実機またはシミュレータでアプリが起動し、空のホーム画面が表示される
- **依存関係**: なし

### T-002: テーマ・デザイントークン実装
- **関連要件**: 要件定義書4章（ビジュアル方針）
- **完了条件**:
  - `core/theme/app_theme.dart` に、コーラル系（クリーム×テラコッタ）の淡いトーンで統一した `ColorScheme`（ライト/ダーク両方）が定義されている
  - カード角丸20〜24px、ボタン/タグのピル形状、フレーム40px相当の共通定数/コンポーネントスタイルが定義されている
  - フラットデザイン方針（強い影・グラデーションの多用を避ける）に沿った `ThemeData` が構成されている
  - シミュレータのシステム設定でライト/ダークを切り替え、アプリが自動追従することを目視確認済み
- **依存関係**: T-001

### T-003: palette_generatorによる代表色抽出のプロトタイプ検証
- **関連要件**: tech-stack.md 2章、要件定義書5.2・6章Must「写真からの代表色抽出」、9章検討事項
- **完了条件**:
  - 明るい写真・暗い写真・彩度の低い写真など複数種のサンプル画像に対し、`palette_generator` でドミナントカラーを抽出する簡易検証コード（画面 or スクリプト）を用意し実行済み
  - 抽出結果が視覚的に妥当であるかを確認し、精度が不十分な場合の代替方針（自前減色ロジックへの切り替え要否）を判断済みである
  - 検証結果・判断内容が記録されている（コード内コメント、または別途メモとして残す）
- **依存関係**: T-001

---

## フェーズ1: データ層

### T-004: Driftデータベーススキーマ実装
- **関連要件**: 要件定義書7章（データ設計）、design.md 4章
- **完了条件**:
  - design.md 4章で定義した `theme_catalog` / `daily_themes` / `entries` / `settings` の4テーブルが Drift の `Table` クラスとして実装され、`AppDatabase`（`schemaVersion = 1`）がコード生成込みでビルド成功する
  - `daily_themes`・`entries` の両テーブルに論理削除用の `deletedAt`（nullable DateTime）カラムが初期スキーマから含まれている（確定事項、design.md 4章冒頭）
  - `entries.recordedAt` の降順インデックス、`entries.dailyThemeId` のインデックスが設定されている
  - 各テーブルへのinsert/query/watchが行える最小限のDAOが実装され、簡単な単体テスト（insert→select）が通る
  - DAOの一覧・集計系クエリ（timeline、ribbon、地図用フェッチ等の元になるメソッド）が `deletedAt IS NULL` を条件に含む形で実装されている
- **依存関係**: T-001
- **補足**: 実装未着手の段階での確定事項反映のため、既存データのマイグレーションは発生しない（初期スキーマにそのまま含める）。

### T-005: お題カタログ初期データ作成・シード投入
- **関連要件**: 要件定義書4章（お題の出し方）、6章Must（お題の生成・表示）、9章検討事項（固定リストの規模）、design.md 4.1節・4.5節
- **完了条件**:
  - 情緒的な色名・代表色・季節タグ・天気タグ・詩的文言バリエーションを持つお題カタログJSONを**60件**作成する。季節（春夏秋冬）ごとに約15件（季節限定タグを持つテーマ＋季節非依存の汎用テーマを含む）を目安に配分する
  - 天気タグの値は design.md 4.5節で定義した内部語彙（`sunny` / `cloudy` / `rain` / `snow` / `fog` / `thunder`、または天気非依存を表す空配列）のみを使用する
  - アプリ初回起動時に `ThemeRepository.seedCatalogIfEmpty()` が呼ばれ、`theme_catalog` テーブルへ60件が投入されることを確認
  - 色名だけでなく質感・季節・場所等の言葉を添えた表現になっており、8章アクセシビリティ要件（色覚多様性への配慮）を満たしていることをレビュー済み
- **依存関係**: T-004

---

## フェーズ2: ドメイン・サービス層

### T-006: ThemeSelector（お題選定ロジック）実装
- **関連要件**: design.md 5.3節、要件定義書4章（季節・天気連動の出し分け）
- **完了条件**:
  - `ThemeSelector.getOrCreateTheme()` が design.md 5.3節の選定手順（1.同日冪等性 → 2.季節タグ絞り込み → 3.天気タグ絞り込み → 4.直近7日重複除外の上でランダム選定 → 5.候補0件時は天気タグ抜きで季節のみに緩和 → 6.それでも0件なら季節タグも外す）の通りに実装されている
  - 以下がユニットテストで検証済み
    - 同一日に複数回呼んでも同じ `DailyTheme` が返る（冪等性）
    - 季節タグ・天気タグに応じた絞り込みが行われる
    - 天気情報が取得できない場合、または絞り込み後の候補数が閾値未満の場合に季節タグのみへ正しくフォールバックする
    - 直近7日間に選定済みの `catalogId` が再選定されない
  - `fetchPastThemes(page, pageSize)` が2週間（14日）単位のページネーション仕様（`ThemePage`、`hasMore`フラグ含む）で実装されている
- **依存関係**: T-004, T-005

### T-007: WeatherService実装（Open-Meteo連携）
- **関連要件**: tech-stack.md 5章・8.1節、要件定義書4章、design.md 4.5節
- **完了条件**:
  - 緯度経度を受け取りOpen-Meteo APIから天気（WMOコード）を取得し、design.md 4.5節のマッピング表に従い内部weatherTag（`sunny`/`cloudy`/`rain`/`snow`/`fog`/`thunder`）に変換する `WeatherService.fetchCurrentCondition()` が実装されている
  - オフライン・APIエラー・タイムアウト時に例外を握りつぶし `null` を返すことを確認済み（確定方針：季節のみへフォールバック）
  - design.md 7.1の方針通り、呼び出し元（ThemeSelector）以外から座標が渡らない構造になっている
- **依存関係**: T-001

### T-008: LocationService実装（位置情報取得・パーミッション制御）
- **関連要件**: 要件定義書5.2（位置情報トグル）、8章（プライバシー）
- **完了条件**:
  - `geolocator` をラップした `LocationService.getCurrentPositionIfEnabled(bool userToggleOn)` が実装されている
  - `userToggleOn=false` の場合は `geolocator` の呼び出し自体が発生しないことをコードレビュー/テストで確認
  - パーミッション拒否・取得失敗時に例外を投げず `null` を返すことを確認済み
- **依存関係**: T-001

### T-009: NotificationService実装（ローカル通知基盤）
- **関連要件**: tech-stack.md 6章、要件定義書8章（通知設計）
- **完了条件**:
  - `flutter_local_notifications` の初期化（iOS/Android権限リクエスト含む）、指定日時への通知スケジュール登録、全キャンセルの各メソッドが実装されている
  - iOS/Androidシミュレータ・実機で1件のローカル通知が指定時刻に表示されることを確認済み
- **依存関係**: T-001

---

## フェーズ3: ホーム画面・記録画面（MVP Must中核）

### T-010: ホーム画面UI実装
- **関連要件**: 要件定義書5.1
- **完了条件**:
  - 今日の日付、今日のお題（色スウォッチ＋お題名＋詩的な一言）、「色を見つけたら記録する」ボタン、「気になる色をもう一度探す」リンクが表示される
  - `todayThemeProvider` 経由でThemeSelectorの結果が反映される
  - デザインはT-002のテーマトークンに準拠している
  - ローディング中・エラー時（design.md 7.2の方針に沿った否定しない文言）の表示がある
- **依存関係**: T-002, T-006

### T-011: 記録画面UI実装（撮影〜代表色抽出プレビュー）
- **関連要件**: 要件定義書5.2
- **完了条件**:
  - `image_picker` でカメラを起動し撮影ができる
  - 撮影後 `ColorExtractionService` で代表色を抽出しプレビュー表示する（表示のみ、正誤判定なし）
  - 今日（または選択中）のお題タグが非活性表示される
  - 抽出失敗時はプレースホルダー表示＋否定しない文言で、保存自体はブロックされない
- **依存関係**: T-003, T-010

### T-012: 記録画面UI実装（メモ・位置トグル・保存）
- **関連要件**: 要件定義書5.2、8章（プライバシー：位置情報デフォルトOFF）
- **完了条件**:
  - 一言メモ入力欄（任意）、位置情報記録トグル（デフォルトOFF）、「この色を日記に加える」保存ボタンが実装されている
  - 保存時に `EntryRepository.createEntry` が呼ばれ `entries` テーブルにレコードが作成される
  - トグルOFFのまま保存しても正常に完了し、他機能に支障がないことを確認済み
  - 保存失敗時（DB/ファイルI/Oエラー）に入力内容を保持したまま再試行できる
- **依存関係**: T-004, T-008, T-011

### T-013: 「気になる色をもう一度探す」実装
- **関連要件**: 要件定義書4章、5.1、design.md 5.3節・5.5節・6.1節
- **完了条件**:
  - ホームから `ThemePickerSheet` を開き、`pastThemesProvider(page)` を用いて過去の `daily_themes` を2週間（14日）単位のページネーションで一覧表示できる
  - `hasMore=true` の場合に「もっと見る」等の操作でさらに古い14日分を追加読み込みできる
  - 選択したお題を紐付けて記録画面に遷移し、そのお題IDでエントリを保存できる
- **依存関係**: T-006, T-012

---

## フェーズ4: 色の日記（MVP Must + Should）

### T-014: 色の日記画面（時系列一覧）実装
- **関連要件**: 要件定義書5.3、6章Must
- **完了条件**:
  - `entries` を日時降順で一覧表示（色スウォッチ＋日付＋お題名＋メモ）するリストビューが実装されている
  - `EntryRepository.watchTimeline()` の更新がリアルタイムに反映される（新規記録後、画面遷移し直さずとも一覧に追加される）
  - 論理削除（`deletedAt != null`）されたエントリが一覧に表示されないことを確認済み
- **依存関係**: T-004, T-012

### T-015: グラデーションリボン表示実装
- **関連要件**: 要件定義書5.3、6章Should
- **完了条件**:
  - 直近1週間分の記録色を並べたグラデーションリボンウィジェットが実装されている
  - 色の日記画面でリスト表示／リボン表示をピル形状のトグルで切り替えられる
- **依存関係**: T-014

---

## フェーズ5: 設定・通知（MVP Should）

### T-016: 設定画面UI実装
- **関連要件**: 要件定義書5.5
- **完了条件**:
  - 通知ON/OFFスイッチ、頻度選択（`off` / `occasionally`（デフォルト）/ `daily` の3値。「毎日」ではなく「たまに」等の控えめな選択肢をデフォルトに含む）、位置情報利用可否スイッチ、「データのエクスポート」ボタン、「お題一覧の閲覧」、「アプリについて／バージョン情報」が表示される
  - `SettingsRepository` を通じてDBと連動し、変更が即座に反映・永続化される
- **依存関係**: T-004, T-005

### T-017: 通知スケジューリング実装
- **関連要件**: 要件定義書4章（催促ではなく誘い）、6章Should、8章（通知設計）、design.md 5.3節
- **完了条件**:
  - `NotificationScheduler.rescheduleFromSettings()` が実装され、設定画面での頻度変更時に既存スケジュールが再構築される
  - 頻度ごとの数値仕様（確定）が正しく実装されている
    - `off`: 通知をスケジュールしない
    - `occasionally`: 週2〜3回、9:00〜20:00の間でランダムな時刻に配信し、前回通知から最低24時間間隔を空ける
    - `daily`: 1日1回、8:00〜21:00の間でランダムな時刻に配信
  - 通知文言が「誘い」の複数バリエーションからランダムに選ばれ、催促的な表現になっていないことをレビュー済み
- **依存関係**: T-009, T-016

---

## フェーズ6: 色の地図（MVP Could）

### T-023: MapTiler APIキー取得・設定
- **関連要件**: tech-stack.md 4章・8.2節（地図タイルサービスの確定事項）
- **完了条件**:
  - MapTiler Cloudの無料アカウントを作成し、APIキーを取得済み
  - APIキーをソースコードにハードコードせず、`--dart-define`等のビルド時環境変数、または `.env`＋`.gitignore`等の手段で管理する仕組みが用意されている
  - 取得したAPIキーで実際にタイルが1枚取得できることを疎通確認済み（curl等での確認で可）
- **依存関係**: T-001

### T-018: 色の地図画面実装
- **関連要件**: 要件定義書5.4、6章Could、design.md 6.4節
- **完了条件**:
  - `flutter_map` の `TileLayer` にMapTiler CloudのタイルURL・APIキーを設定し、地図タイルが表示される
  - `locationRecorded=true` かつ `deletedAt IS NULL` の `entries` のみを地図上に色ドットとして表示する
  - ドットタップでその場所の記録（色・日付・お題名）をポップアップ表示する
  - 位置情報付き記録が0件の場合、否定しない空状態メッセージが表示される
  - 地図タイル取得失敗時（通信エラー・無料枠超過等）に他機能へ影響を与えず穏やかにエラーを示すことを確認済み
- **依存関係**: T-008, T-014, T-023

---

## フェーズ7: エクスポート・仕上げ・配布準備

### T-019: データエクスポート機能実装
- **関連要件**: 要件定義書5.5、9章（シェア機能の前段）、design.md 5.4節
- **完了条件**:
  - `entries`・`daily_themes` をJSON化し、参照する画像ファイルとともにアーカイブ（zip等）を作成する `ExportService.exportAndShare()` が実装されている
  - 論理削除済み（`deletedAt != null`）のレコードがエクスポート対象から除外されることを確認済み
  - サイズ上限は設けない方針（確定）に沿い、件数・容量による打ち切り処理を入れていないことを確認済み
  - `share_plus` によりOS標準の共有シートから保存・送信できる
  - エクスポートしたアーカイブを展開し、要件7章のデータ項目（日時・写真・抽出色・お題・メモ・位置情報）が過不足なく含まれていることを確認済み
- **依存関係**: T-014

### T-020: アクセシビリティレビュー・調整
- **関連要件**: 要件定義書8章（アクセシビリティ）
- **完了条件**:
  - お題カタログ（T-005）・各画面の表示において、色名だけに依存せず質感・季節・場所等の言葉が併記されているかをレビューし、必要な文言修正を反映済み
  - 色スウォッチ表示部分に、色以外の情報（お題名テキスト等）が併記されていることを確認済み
- **依存関係**: T-005, T-010

### T-021: エラーハンドリング・フォールバック文言の統一実装
- **関連要件**: design.md 7.2節、要件定義書4章（トーン&マナー）
- **完了条件**:
  - 位置情報取得失敗、天気API失敗、代表色抽出失敗、DB書き込み失敗、画像保存失敗、地図タイル取得失敗の各ケースについて、design.md 7.2の表に沿った統一トーンのメッセージ・フォールバック挙動が実装されている
  - 各ケースを擬似的に発生させて（オフライン化、パーミッション拒否等）動作を確認済み
- **依存関係**: T-007, T-008, T-012, T-018

### T-022: MVPシナリオ通し確認
- **関連要件**: 要件定義書6章Must一式
- **完了条件**:
  - 「起動 → お題表示 → 撮影 → 代表色抽出 → 保存 → 色の日記で確認」の一連のMVPシナリオが実機/シミュレータ（iOS・Android双方）で問題なく完了することを確認済み
  - 位置情報トグルOFFのまま一連の操作を行っても支障がないことを重ねて確認済み
- **依存関係**: T-010, T-011, T-012, T-014

### T-024: iOS配布準備（Info.plist・Privacy Manifest・App Store Connectプライバシー申告）
- **関連要件**: tech-stack.md 8.3節（iOSストア対応、確定・対応必須）
- **完了条件**:
  - `Info.plist` に `NSLocationWhenInUseUsageDescription`（tech-stack.md記載の文言案を反映）、`NSCameraUsageDescription` 等、必要な利用目的説明文言が設定されている。`NSLocationAlwaysAndWhenInUseUsageDescription` は設定しない
  - 依存パッケージ（geolocator, path_provider, sqlite3_flutter_libs/drift, flutter_local_notifications, image_picker等）を最新安定版に揃えた上で、各パッケージが同梱する `PrivacyInfo.xcprivacy` の有無・内容を確認し、`flutter_privacy_doctor` 等での監査結果に不足があればアプリ側の `PrivacyInfo.xcprivacy` にReason Codeを追記済み
  - App Store Connectのプライバシー申告（Privacy Nutrition Label）で、位置情報について「データ収集あり／トラッキングなし／ユーザーへの紐付けなし／用途はApp Functionality」として申告する準備が整っている
  - App Tracking Transparency（ATT）の同意ダイアログを実装していないこと、およびその判断根拠（トラッキングを行わないため不要）がドキュメント化されている
- **依存関係**: T-008, T-012, T-018（位置情報・地図関連機能が実装済みであること）

---

## タスク依存関係サマリー

```
T-001 ─┬─> T-002 ───────────────────────────────────┐
       ├─> T-003 ────────────────────────┐        │
       ├─> T-004 ─┬─> T-005 ─┬─> T-006 ┤        │
       │          │          └────────┼────────┼─> T-010 ─> T-011 ─> T-012 ─> T-013
       │          └─────────────────────┼────────┘                        │
       ├─> T-007 ─────────────────────────────────────────────┤(T-021)
       ├─> T-008 ─────────────────────────────────────────────┤(T-012, T-018, T-021, T-024)
       ├─> T-009 ───────────────────────> T-017
       └─> T-023 ───────────────────────> T-018

T-012 ─> T-014 ─┬─> T-015
                ├─> T-018 (T-008, T-023も依存)
                └─> T-019

T-005 ─┬─> T-016 ─> T-017
       └─> T-020 (T-010も依存)

T-007, T-008, T-012, T-018 ─> T-021
T-010, T-011, T-012, T-014 ─> T-022 （MVPシナリオ通し確認）
T-008, T-012, T-018 ─> T-024 （iOS配布準備）
```

**最初に着手すべきタスク**: T-001（Flutterプロジェクト初期セットアップ）。並行してT-003（palette_generator精度検証）はT-001完了後すぐ着手可能で、MVP最大のリスク要因（代表色抽出の実用精度）を早期に検証できるため優先度を高く設定している。T-023（MapTiler APIキー取得）はCould機能である地図画面（T-018）の直前でよく、MVP Mustフェーズの進行を妨げない。
