# そぞろいろ 技術選定書

作成日: 2026-08-16
更新日: 2026-08-17（未決事項に対するユーザー決定事項を反映）
対象: 要件定義書5章〜9章を踏まえた実装技術スタックの選定
想定開発体制: 個人開発、小〜中規模アプリ

---

## 0. 選定の基本方針

要件定義書の世界観（急かさない・比べない・プライバシー配慮）と、9章で明記されている検討事項（お題生成方式、代表色抽出方式、技術スタック選定）を踏まえ、以下を選定の軸とする。

- **オフラインファースト**: 位置情報デフォルトOFF、通知は控えめ、コア機能（記録・日記閲覧）はネットワーク非依存という非機能要件（8章）と整合させる。
- **単一開発者で iOS/Android 両対応**: クロスプラットフォームを前提とし、二重実装を避ける。
- **過剰投資の回避**: 自前サーバー・認証基盤・複雑な状態管理は、MVPの要求（6章）に対して不要と判断される限り採用しない。
- **カスタムビジュアル表現への対応力**: 角丸カード、ピル形状、グラデーションリボン表示など、4章のデザイン方針を素直に実装できる描画力を重視する。

---

## 1. モバイルアプリフレームワーク

### 選定: **Flutter (Dart)**

| 選択肢 | 評価 |
|---|---|
| **Flutter** | ◎ 単一コードベースでiOS/Android両対応。Skiaベース（新エンジンはImpeller）の自由な描画力があり、グラデーションリボン表示・柔らかい角丸カード・ピル形状ボタンなど、独自性の強いUIをネイティブウィジェットの制約なく実装できる。個人開発向けパッケージ（DB、位置情報、通知、地図等）が揃っている。 |
| React Native | △ JS/TSエコシステムは魅力だが、今回のようなカスタム描画（リボン表示のグラデーション、丸い縁取りスウォッチ等）はネイティブブリッジやSVG/Skiaライブラリの追加が必要になりやすく、Flutterより実装コストが高い。 |
| Kotlin Multiplatform | △ ロジック共有はできてもUIは基本的にiOS/Android別実装が必要で、個人開発が一人で担うには工数が単純に倍増する。今回のように「見た目」が体験の核であるアプリには不向き。 |
| ネイティブ別実装（Swift + Kotlin） | × 学習・実装・保守コストが最も高い。個人開発でMVPを素早く出す方針と矛盾する。 |

**理由まとめ**: このアプリは機能の複雑さよりも「柔らかく可愛らしい独自ビジュアル」が価値の中心（4章）であり、Flutterの宣言的UI・自由な描画力・単一コードベースが最も要件に適合する。

**補助ライブラリ**
- 状態管理: `Riverpod`（学習コストが低く型安全、個人開発でもテストしやすい）
- ルーティング: `go_router`
- テーマ: Material 3 の `ColorScheme` でクリーム×テラコッタの淡いトーンを定義し、`ThemeMode.system` でライト/ダーク自動追従

---

## 2. 写真からの代表色抽出（9章の検討事項）

### 選定: **オンデバイス処理**

**理由**
- プライバシー重視の世界観（位置情報デフォルトOFF等）と一貫性を持たせるため、撮影した写真を外部サーバーへ送信しない設計とする。
- 散歩中は電波状況が不安定な場面もあり得るため、オフラインで完結できることが体験上望ましい。
- 個人開発においてサーバー運用（画像処理API、ストレージ、レスポンス待ち時間）のコストとレイテンシを避けられる。
- 求められている抽出精度は「正誤判定はしない、表示のみ」（5.2）であり、高度な画像認識は不要。ドミナントカラー抽出程度で要件を満たす。

**実装方針**
- Flutterパッケージ `palette_generator`（撮影画像から代表色パレットを算出。Android Palette APIのDart実装）を第一候補とする。
- 精度・パフォーマンスに不満が出た場合は、画像を軽量にリサイズした上で自前の減色処理（k-meansの簡易版、または色ヒストグラム最頻値抽出）に置き換える拡張余地を残す。
- 抽出処理は撮影直後、記録画面内でその場で実行し、結果（カラーコード）をプレビュー表示する。

---

## 3. データ永続化

### 選定: **ローカルDBのみ（クラウド同期は行わない）／バックエンド不要**

**理由**
- 7章のデータ設計（Theme, Entry）はいずれも個人のローカル記録であり、複数ユーザー間の共有・照合は不要。
- 6章MVP・Should・Could機能を見ても、クラウド保存や複数端末同期は要求されていない。5.5「データのエクスポート」はあるが、これはローカルデータをユーザー自身が書き出す機能であり、クラウド同期とは別物。
- 個人開発でサーバー・DB運用・認証基盤を持つのはMVPに対して過剰投資。

**実装方針**
- ローカルDB: **Drift**（型安全なSQLiteラッパー、旧Moor）
  - お題（Theme）・記録エントリ（Entry）はリレーショナルに素直に表現でき、日付範囲検索や週/月集計（グラデーションリボン表示、5.3）に強いSQLクエリが使える点を評価。
  - 代替候補として `Isar`（NoSQL的、高速）も検討したが、SQLの方が「週単位集計」「地図用の位置情報付きレコード抽出」等の柔軟なクエリを書きやすく、個人開発でも学習コストが低いためDriftを優先。
- 画像本体: アプリのドキュメントディレクトリ（`path_provider`で取得）にファイルとして保存し、DriftのEntryテーブルにはファイルパスと抽出カラーコードのみ保持。
- エクスポート機能（5.5）: 記録データをJSON（+ 画像ファイル一式）にまとめ、`share_plus` でOS標準の共有シートから保存・送信できるようにする。これが実質的な「バックアップ手段」を兼ねる。サイズ上限は設けない（確定方針。詳細は8.5のストレージ課題を参照）。
- **バックエンド**: 不要と判断。天気連動お題（後述）も含め、すべてクライアント単体・外部公開APIの直接呼び出しで完結させる。

---

## 4. 位置情報・地図表示

### 選定: **geolocator（位置情報取得）＋ flutter_map + MapTiler Cloud（地図表示、確定）**

| 選択肢 | 評価 |
|---|---|
| **MapTiler Cloud（ホスティング型タイル、採用）** | ◎ 無料枠（月10万タイルロード）が個人開発の想定規模には十分。OpenStreetMapベースの地図データを利用規約に沿った形で配信でき、`flutter_map`の`TileLayer`はそのまま流用し、`urlTemplate`とAPIキーのみをMapTilerのものに差し替えれば実装できる。 |
| 生のOpenStreetMap標準タイル（`tile.openstreetmap.org`） | × 不採用（確定）。OSMFの利用規約上、標準タイルサーバーは「評価・軽量用途限定」であり、一般配布アプリでの本番利用は想定されていない。利用量が増えるとレート制限・ブロックの対象になり得るため、個人開発であっても配布アプリとして採用するのは適切でないと判断した。 |
| Google Maps (`google_maps_flutter`) | △ 表示品質は高いが、APIキー管理・利用量に応じた課金設計・iOS側のGoogle Maps SDK同梱対応など、個人開発の運用負荷が増す。今回の用途には過剰。 |
| Apple Maps (`apple_maps_flutter`) | × iOS専用でAndroidに使えず、クロスプラットフォーム方針と矛盾する。 |

**理由まとめ**: 5.4の要件は「地図上に色のドットを表示し、タップでポップアップ」という単純なもので、無課金枠で完結できるホスティング型タイルサービスが個人開発に最も適する。当初はAPIキー不要な生のOSMタイルを想定していたが、利用規約上「本番配布アプリでの利用は想定外」と明記されているため、**MapTiler Cloud**（OpenStreetMapベースのデータを商用利用規約に沿って配信するホスティングサービス、無料枠あり）に切り替える。

**実装方針**
- `flutter_map`のウィジェット構成・依存パッケージ自体は変更しない。`TileLayer`の`urlTemplate`をMapTilerの発行するタイルURL（例: `https://api.maptiler.com/maps/{style}/{z}/{x}/{y}.png?key={apiKey}`形式）に差し替え、APIキーを`--dart-define`等のビルド時環境変数として注入する。
- 地図画面自体は6章でCould（後回し可）機能のため、MapTilerの無料アカウント登録・APIキー取得は地図画面の実装着手時に行えばよい（実装タスクは `docs/tasks.md` に追記済み）。
- 無料枠（月10万タイルロード）を超過した場合の挙動・アップグレード要否は、リリース後の利用状況を見て判断する（将来課題）。

**位置情報取得**: `geolocator` パッケージで現在地取得・パーミッション管理を行う。5.2のトグルがOFFの場合は取得処理自体を呼び出さない実装とし、「OFFでも他機能に支障がない」という要件を構造的に担保する。

---

## 5. バックエンドの要否

### 結論: **不要（サーバーレス構成）**

**理由**
- コア体験（お題表示・撮影記録・日記閲覧・地図表示）はすべて7章のデータ設計に基づきローカルのみで完結可能。
- お題の「季節・天気連動での出し分け」（4章）は、固定リスト（アプリ内蔵JSON等）＋端末の現在日時（季節判定）＋天気情報のみで実現でき、天気情報は無料・APIキー不要の **Open-Meteo API** をクライアントから直接呼び出せば十分。専用バックエンドを介す必要はない。
- 認証・ユーザー管理機能はMVP〜Could機能のいずれにも存在しない（複数端末同期や共有機能が明記されていないため）。
- 個人開発でサーバーの構築・運用・監視・セキュリティ対応を抱えるのは、この規模のアプリには明確に過剰投資。

**天気APIのオフライン時フォールバック（確定方針）**: 圏外・機内モード等でOpen-Meteo APIが呼び出せない場合は、例外を握りつぶし季節タグのみでお題の出し分けを行う。これはクライアント側のみで完結する処理であり、フォールバックのためにサーバーを用意する必要はない（設計詳細は `docs/design.md` の `ThemeSelector` / `WeatherService` を参照）。

**将来的にバックエンドが必要になり得るケース（現時点では非採用）**
- 複数端末間でのデータ同期・機種変更時の自動引き継ぎ
- 年間まとめのシェア用ビジュアル生成をサーバー側でレンダリングする場合（Could機能）
- お題生成ロジックの高度化（9章）でサーバー側にコンテンツ管理を持たせたくなった場合（現時点では検討しないことを確定。8.3参照）

これらが必要になった際は、自前サーバーより **Firebase（Firestore + Authentication + Cloud Functions）等のBaaS** を後付けする方が、個人開発の運用負荷という観点で適切。現時点では設計・実装しない。

---

## 6. 通知の実装

### 選定: **端末ローカル通知（`flutter_local_notifications`）のみ、プッシュ通知基盤は不使用**

**理由**
- 8章「通知設計」は頻度をユーザーが選べる控えめな仕組みであり、サーバー起点でリアルタイムに送る必要性がない（お題は固定＋日次で決まるため、サーバーからのプッシュで即時性を持たせる意味が薄い）。
- FCM等のプッシュ通知基盤を導入すると、送信サーバー・デバイストークン管理などの運用が発生し、個人開発の規模に見合わない。

**実装方針**
- `flutter_local_notifications` で端末内にスケジュール通知を設定。
- 設定画面（5.5）の頻度選択（「毎日」「たまに」等）に応じて、通知タイミングをランダム化しつつローカルにスケジュール。頻度ごとの具体的な時間帯・間隔の数値仕様は確定済み（`docs/design.md` 5.3節「通知スケジューリング」を参照）。
- 通知文言は「催促」ではなく「誘い」の複数バリエーションをアプリ内に静的データとして持たせ、ランダムに選択して表示する。

---

## 7. 技術スタックまとめ

| レイヤ | 採用技術 | 備考 |
|---|---|---|
| フレームワーク | Flutter (Dart) | iOS/Android共通コードベース |
| 状態管理 | Riverpod | |
| ルーティング | go_router | |
| ローカルDB | Drift (SQLite) | お題・記録エントリの永続化 |
| 画像保存 | ファイルシステム (`path_provider`) | Drift側はパス参照のみ |
| カメラ/撮影 | `image_picker`（カメラソース） | カスタムカメラUIが必要になれば`camera`パッケージへ移行 |
| 代表色抽出 | `palette_generator`（オンデバイス） | 精度不足時は自前減色ロジックへ拡張 |
| 位置情報 | `geolocator` | デフォルトOFF、呼び出し自体をトグルで制御 |
| 地図表示 | `flutter_map` • **MapTiler Cloud**（無料枠: 月10万タイルロード） | 生のOSM標準タイルは配布アプリでは利用規約上不採用（確定） |
| 通知 | `flutter_local_notifications` | プッシュ基盤なし。頻度別スケジュール仕様は`docs/design.md`参照 |
| 天気情報 | Open-Meteo API（クライアント直叩き） | APIキー不要、専用バックエンド不要。オフライン時は季節タグのみにフォールバック（確定） |
| データエクスポート | `share_plus` • JSON/画像出力 | クラウドバックアップの代替。サイズ上限なし（確定） |
| バックエンド | なし（サーバーレス） | 将来必要ならFirebase等のBaaSを後付け |

---

## 8. 確定事項（前回の未決事項に対するユーザー決定・2026-08-17反映）

### 8.1 天気APIのオフライン時フォールバック（確定）
圏外・機内モード時は季節タグのみで出し分ける。既存の「`WeatherService`が`null`を返した場合は`ThemeSelector`が季節のみでフォールバックする」という設計方針（`docs/design.md` 5.3/7.2節）を正式決定とする。追加のオフラインキャッシュ等は実装しない。

### 8.2 地図タイルサービス（確定）
上記4章の通り、**MapTiler Cloud**（無料枠: 月10万タイルロード）を採用する。生のOpenStreetMap標準タイルは配布アプリでの本番利用が利用規約上想定されていないため不採用。実装（`flutter_map`）自体は変更なく、TileLayerの向き先とAPIキーのみの差し替えで対応する。

### 8.3 iOSストア対応（確定・対応必須）
MVPで位置情報機能（5.2トグル、5.4地図）を含める以上、iOS配布に向けて以下の対応を必須とする。

**(1) Info.plist — 位置情報の利用目的説明文言**
- 要求する権限は `NSLocationWhenInUseUsageDescription` のみとする。`NSLocationAlwaysAndWhenInUseUsageDescription`（Always権限）は要求しない。位置情報は記録画面でユーザーがトグルをONにした場面でのみ、その都度 When In Use 権限で取得すれば要件を満たせるため（バックグラウンド追跡や常時測位は行わない）。
- 文言案（4章のトーン&マナーに準拠した「急かさない・否定しない」表現）:
  > 「歩いた場所と見つけた色を結びつけて記録するために使用します。記録するかどうかは、記録画面でいつでもオン・オフを選べます。」
- 参考: カメラ撮影（`image_picker`）を行うため `NSCameraUsageDescription` も別途必要（例:「見つけた色を撮影するために使用します。」）。写真ライブラリへの保存は行わない設計であれば `NSPhotoLibraryAddUsageDescription` は原則不要だが、実装方式（撮影のみか、ライブラリ選択も許可するか）が確定した時点で再確認する。

**(2) Apple Privacy Manifest（`PrivacyInfo.xcprivacy`）対応**
Appleは2024年以降、Required Reason API（5カテゴリ: File Timestamp / User Defaults / System Boot Time / Disk Space / Active Keyboards）を使用するアプリ・SDKに対し、有効な理由コードを含むプライバシーマニフェストの同梱を審査時に要求している（[Apple公式ドキュメント TN3183](https://developer.apple.com/documentation/technotes/tn3183-adding-required-reason-api-entries-to-your-privacy-manifest)）。本アプリで使用予定のプラグインについて、現時点で判明している対応状況・想定される申告内容は以下の通り。

| プラグイン | Required Reason API該当の可能性 | 対応方針 |
|---|---|---|
| `geolocator`（`geolocator_apple`） | 位置情報データの収集自体が `NSPrivacyCollectedDataTypes` の申告対象。パッケージ自体が[PrivacyInfo.xcprivacyを同梱済み](https://github.com/Baseflow/flutter-geolocator/blob/main/geolocator_apple/ios/Resources/PrivacyInfo.xcprivacy)であることを確認済み。 | アプリ側では、この位置情報収集をアプリレベルの`NSPrivacyCollectedDataTypes`（下記(3)参照）として申告する。 |
| `path_provider` | ファイルタイムスタンプAPI (`NSPrivacyAccessedAPICategoryFileTimestamp`) を利用している可能性がある | 実装着手時（ビルド前）に最新バージョンの同梱マニフェストを確認 |
| `sqlite3_flutter_libs` / `drift` | ディスク容量API・ファイルタイムスタンプAPIを利用している可能性がある | 同上 |
| `flutter_local_notifications` | UserDefaults API (`NSPrivacyAccessedAPICategoryUserDefaults`) を利用している可能性がある | 同上 |
| `image_picker` | ファイルタイムスタンプAPIを利用している可能性がある | 同上 |

**対応方針（確定）**: 上記プラグインの多くは2024年以降のバージョンで自身のPrivacyInfo.xcprivacyを同梱するようになっているため、実装フェーズでは (a) 各依存パッケージを最新安定版に揃える、(b) `flutter_privacy_doctor`（Flutter依存関係のPrivacyInfo.xcprivacy対応状況を監査し、App Store Privacy Labelの生成を補助するツール）等で不足を検出する、(c) 不足があればアプリ本体の`PrivacyInfo.xcprivacy`にReason Code（Appleの承認済みコード一覧から実態に最も近いものを選定）を追記する、という手順で対応する。プラグインのバージョンによって内容が変わるため、確定的な申告内容はビルド直前に再確認する運用とする。

**(3) App Store Connect のプライバシー申告（Privacy Nutrition Label）**
位置情報を、お題選定のためOpen-Meteo APIへ送信する（8.1・4章参照）ため、以下の申告を行う（確定方針）。
- **データ収集: あり**（Location — Precise or Coarse Locationのいずれかを、取得実装の精度指定に応じて選択）
- **トラッキング: なし**（第三者の広告ネットワーク等への横断的なトラッキングは行わない）
- **ユーザーへの紐付け: なし**（アカウント機能を持たないため、収集した位置情報を特定ユーザーIDに紐付けて外部に送信することはない。Open-Meteoへの送信は匿名の座標クエリのみ）
- 用途区分は「App Functionality（アプリ機能の提供）」として申告する。

**(4) App Tracking Transparency（ATT）**
不要（確定）。本アプリは他社アプリ・Webサイトを横断したトラッキングを一切行わないため、`AppTrackingTransparency`フレームワークによる同意ダイアログの実装は不要と判断する。

---

## 9. 引き続き将来課題とする事項（現時点では対応しない）

以下は検討はしたが、MVP〜当面のスコープでは対応しないことが確定した事項。将来、実際の利用状況を踏まえて再検討する。

- **ストレージ肥大化対策**: 写真をオンデバイス保存し続けることによる長期利用時の容量増加（画像の自動圧縮・リサイズ、古い記録の扱い等）は、現時点では対策を行わない。
- **機種変更時のデータ移行（インポート機能）**: エクスポート機能（JSON+画像）は実装するが、対となるインポート機能は将来実装予定としMVPスコープ外とする。
- **お題生成ロジックの高度化（9章）**: 固定リスト（60件、`docs/design.md` 参照）のロジック高度化・動的生成は検討しない。MVPは固定リストのままでよい。

---

## 10. 次のステップ（実装フェーズへの引き継ぎ）

本ドキュメントおよび `docs/design.md`・`docs/tasks.md` の技術選定・確定事項を前提に実装を進める。着手すべき最初の技術検証は以下の2点（変更なし）。

1. Flutterプロジェクトの初期セットアップとテーマ（角丸・ピル形状・コーラル系カラースキーム）の実装確認
2. `palette_generator` による代表色抽出の精度・パフォーマンス検証（実機カメラ画像でのプロトタイピング）

地図機能（Could）に着手する際は、事前にMapTiler Cloudのアカウント登録・APIキー取得（`docs/tasks.md` 追記タスク参照）が必要になる点に留意する。
